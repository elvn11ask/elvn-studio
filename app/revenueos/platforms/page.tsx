import type { Metadata } from "next";
import { ProductHero, ProductSchema } from "@/components/revenueos/product-shell";

export const metadata: Metadata = { title: "Revenue OS Platform Support", description: "How Revenue OS checks custom, modern, legacy, server-rendered, static and client-rendered websites.", alternates: { canonical: "/revenueos/platforms" } };

const rows = [["Validated", "Next.js; custom server-rendered catalog sites; non-catalog commercial site", "Bounded production evidence exists."], ["Protocol-compatible", "PHP, Java server apps, static HTML, WordPress, WooCommerce, Shopify, Webflow, Framer, Tilda", "Basic HTTP/HTML checks do not require a framework connector; platform-specific validation is still required."], ["Client-rendered JavaScript", "React/Vue/SPA application shells", "HTTP facts work now. Full rendered-DOM, mobile and visual checks require the optional browser adapter, currently under validation."], ["Private or authenticated", "Intranets, customer portals, staging behind login", "Not scanned by the public worker. Requires explicit authorization and a separately configured execution boundary."]];

export default function PlatformsPage(){return <><ProductHero eyebrow="Platform support · No partnership claims" title="Web-contract first. Framework adapters second." lede="Revenue OS primarily observes HTTP, HTML, headers, robots, sitemaps, DOM-visible metadata, schema, links and timing. A CMS or framework integration can add context, but it is not required for the baseline." actions={false}/><section className="section shell"><div className="platform-table">{rows.map(([status,platforms,note])=><article key={status}><span>{status}</span><h2>{platforms}</h2><p>{note}</p></article>)}</div></section><section className="section section-contrast"><div className="shell content-grid"><aside><p className="eyebrow">What “support” means</p><h2>Observable, not magical.</h2></aside><div className="prose"><p>A Java, PHP, Node, Ruby or .NET server that returns public HTML is analyzed through the same web contracts. Static sites work the same way.</p><p>A client-rendered app may return an empty shell. Revenue OS records the HTTP fact, labels <code>CLIENT_RENDER_REQUIRED</code>, and avoids false conclusions until the browser adapter runs.</p><p>“Compatible with” does not mean partner, certified integration or complete validation on every theme and plugin combination.</p></div></div></section><ProductSchema path="/revenueos/platforms" name="Revenue OS platform support" description="Protocol-first support and explicit limitations across modern and legacy web stacks."/></>}
