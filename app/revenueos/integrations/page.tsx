import type { Metadata } from "next";
import { ProductCTA, ProductHero, ProductSchema } from "@/components/revenueos/product-shell";
import { integrations } from "@/lib/revenueos";
export const metadata:Metadata={title:"RevenueOS Integrations",description:"Website, CRM, ERP, communication, and analytics integration options classified by delivery method and discovery needs.",alternates:{canonical:"/revenueos/integrations"}};
export default function IntegrationsPage(){const groups=[...new Set(integrations.map((item)=>item.group))];return <><ProductHero eyebrow="Integration landscape" title="Keep the systems that already perform their job." lede="RevenueOS can sit above the website, CRM, ERP, mailbox, and analytics stack. Every connection is classified honestly before it becomes a delivery claim."/><section className="section shell integration-catalog">{groups.map((group)=><section key={group}><div><p className="eyebrow">Integration group</p><h2>{group}</h2></div><div>{integrations.filter((item)=>item.group===group).map((item)=><article key={`${group}-${item.name}`}><h3>{item.name}</h3><span>{item.classification}</span><p>{item.note}</p></article>)}</div></section>)}</section><section className="section product-attribution"><div className="shell content-grid"><aside><p className="eyebrow">Implementation rule</p><h2>No direct database coupling by default.</h2></aside><div className="prose"><p>Commercial writes use versioned APIs, durable outboxes, webhooks, or controlled file contracts. Idempotency and reconciliation are designed before live traffic is connected.</p><p>“Supported” means the architecture can accommodate the system. “Production-ready” is stated only after credentials, permissions, mapping, retries, and customer acceptance have been validated.</p></div></div></section><ProductCTA/><ProductSchema path="/revenueos/integrations" name="RevenueOS Integrations" description="Integration methods for websites, CRM, ERP, communications, and analytics."/></>}
