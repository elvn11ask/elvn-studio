import type { Metadata } from "next";
import { GraphCTA, GraphHero } from "@/components/revenueos/graph-shell";
import { ProductSchema } from "@/components/revenueos/product-shell";
export const metadata:Metadata={title:"AI Controls for Revenue Knowledge Graph",description:"Optional, read-only, human-reviewed AI recommendations separated from commercial facts.",alternates:{canonical:"/revenueos/knowledge-graph/ai"}};
const controls=["AI is disabled by default and the graph remains fully usable.","Models read a minimum, tenant-scoped context selected by policy.","Every recommendation records provider, model, prompt policy, inputs, output, and time.","Recommendations cannot silently merge products, select suppliers, change price, or contact customers.","Human review is a separate append-only decision with reviewer and reason.","Untrusted text is treated as data and prompt-injection markers are rejected at ingestion."];
export default function Page(){return <><GraphHero eyebrow="Knowledge Graph · Optional AI" title="Recommendations are not facts." lede="Models may summarize evidence, flag gaps, and suggest the next review. Deterministic records and human commercial authority stay in control."/><section className="section shell"><ol className="control-list">{controls.map((x,i)=><li key={x}><span>0{i+1}</span>{x}</li>)}</ol></section><GraphCTA/><ProductSchema path="/revenueos/knowledge-graph/ai" name="Revenue Knowledge Graph AI Controls" description="Optional human-reviewed AI recommendations."/></>}
