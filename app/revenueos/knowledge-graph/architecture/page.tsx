import type { Metadata } from "next";
import { GraphCTA, GraphHero } from "@/components/revenueos/graph-shell";
import { ProductSchema } from "@/components/revenueos/product-shell";
import { graphLayers, graphPrinciples } from "@/lib/revenue-knowledge-graph";
export const metadata:Metadata={title:"Revenue Knowledge Graph Architecture",description:"Append-only events, deterministic identity, provenance, reproducible projections, and operational task views.",alternates:{canonical:"/revenueos/knowledge-graph/architecture"}};
export default function Page(){return <><GraphHero eyebrow="Knowledge Graph · Architecture" title="A projection you can rebuild, reconcile, and explain." lede="Authoritative systems emit versioned events. A tenant-scoped projection connects verified entities and relationships, while task views turn that evidence into operational work."/><section className="section shell"><div className="graph-pipeline">{graphLayers.map(([name,text],index)=><article key={name}><span>0{index+1}</span><h2>{name}</h2><p>{text}</p></article>)}</div></section><section className="section section-contrast"><div className="shell"><div className="section-heading"><div><p className="eyebrow">Non-negotiable contracts</p><h2>Identity, provenance, time, and money are explicit.</h2></div><p>Projection versions and reconciliation checks make change observable rather than silently rewriting history.</p></div><div className="module-preview">{graphPrinciples.map(([name,text],index)=><article key={name}><span>0{index+1}</span><h3>{name}</h3><p>{text}</p></article>)}</div></div></section><GraphCTA/><ProductSchema path="/revenueos/knowledge-graph/architecture" name="Revenue Knowledge Graph Architecture" description="A reproducible commercial relationship projection."/></>}
