import type { Metadata } from "next";
import { GraphCTA, GraphHero } from "@/components/revenueos/graph-shell";
import { ProductSchema } from "@/components/revenueos/product-shell";
export const metadata:Metadata={title:"Revenue Knowledge Graph Security",description:"Tenant isolation, role controls, provenance, data minimization, retention, audit, and recovery principles.",alternates:{canonical:"/revenueos/knowledge-graph/security"}};
const items=[["Tenant isolation","Tenant is part of every event, entity, relationship, token, query, and projection checkpoint."],["Role-based access","Sales, procurement, finance, marketing, integration, auditor, and administrator permissions stay distinct."],["Data minimization","Adapters map only fields needed by approved task views; free text and contact data do not enter analytical projections by default."],["Append-only evidence","Events and AI recommendations cannot be edited in place. Corrections arrive as new events."],["Reconciliation","Counts, identities, source gaps, projection checksums, and financial requirements are checked before promotion."],["Recovery","Backups, replay, projection rebuild, dead-letter handling, and rollback are part of the deployment acceptance plan."]];
export default function Page(){return <><GraphHero eyebrow="Knowledge Graph · Security" title="Commercial visibility without uncontrolled exposure." lede="Security is enforced at event intake, projection, API, task view, and deployment boundaries—not left to a dashboard filter."/><section className="section shell security-grid">{items.map(([a,b],i)=><article key={a}><span>0{i+1}</span><h2>{a}</h2><p>{b}</p></article>)}</section><GraphCTA/><ProductSchema path="/revenueos/knowledge-graph/security" name="Revenue Knowledge Graph Security" description="Tenant-scoped commercial intelligence controls."/></>}
