import type { Metadata } from "next";
import { GraphCTA, GraphHero } from "@/components/revenueos/graph-shell";
import { ProductSchema } from "@/components/revenueos/product-shell";
import { graphViews } from "@/lib/revenue-knowledge-graph";
export const metadata:Metadata={title:"Revenue Knowledge Graph Use Cases",description:"Operational graph views for quotes, attribution, suppliers, product enrichment, margin, exceptions, and account timelines.",alternates:{canonical:"/revenueos/knowledge-graph/use-cases"}};
export default function Page(){return <><GraphHero eyebrow="Knowledge Graph · Use cases" title="Task views for the questions revenue teams actually ask." lede="Each view starts from an owner, next action, evidence state, and deadline. The relationship model supports the work; it is not the interface goal."/><section className="section shell module-catalog">{graphViews.map(([name,text],index)=><article key={name}><span>{String(index+1).padStart(2,"0")}</span><div><h2>{name}</h2><p>{text}</p></div><dl><dt>Evidence</dt><dd>Source record, observed time, mapping version, and fact class</dd><dt>Action</dt><dd>Named owner, next step, exception reason, and review state</dd></dl></article>)}</section><GraphCTA/><ProductSchema path="/revenueos/knowledge-graph/use-cases" name="Revenue Knowledge Graph Use Cases" description="Operational revenue intelligence task views."/></>}
