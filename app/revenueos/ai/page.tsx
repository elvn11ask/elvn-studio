import type { Metadata } from "next";
import { ProductCTA, ProductHero, ProductSchema } from "@/components/revenueos/product-shell";
import { aiCapabilities, aiProviders } from "@/lib/revenueos";
export const metadata:Metadata={title:"Optional BYO AI Architecture",description:"Provider-independent AI assistance for RFQ workflows with explicit human approval and deterministic commercial records.",alternates:{canonical:"/revenueos/ai"}};
const controls=["The core workflow operates fully without AI.","AI is never the system of record.","Exact MPN identity remains deterministic.","Pricing, supplier, compliance, quote terms, and outbound communication require controlled approval.","Customer data is sent only after explicit provider and data-policy configuration.","Provider keys stay encrypted, isolated, and absent from browser bundles and logs."];
export default function AIPage(){return <><ProductHero eyebrow="Bring Your Own AI · Optional" title="Use models for assistance, not commercial authority." lede="Connect a customer-approved provider to summarize, classify, draft, and detect risk while the underlying RFQ, product, cost, quote, and payment records remain deterministic."/><section className="section shell"><div className="section-heading"><div><p className="eyebrow">Provider architecture</p><h2>One governed boundary for cloud, private, and local models.</h2></div><p>Provider availability is an architectural target, not a claim that every connector has already passed production validation.</p></div><div className="provider-grid">{aiProviders.map((provider)=><span key={provider}>{provider}</span>)}</div></section><section className="section section-contrast"><div className="shell"><div className="section-heading"><div><p className="eyebrow">Candidate capabilities</p><h2>Reduce review effort where judgment still belongs to people.</h2></div><p>Every capability is enabled separately with a documented input policy, output status, and approval path.</p></div><div className="module-preview">{aiCapabilities.map((capability,index)=><article key={capability}><span>{String(index+1).padStart(2,"0")}</span><h3>{capability}</h3></article>)}</div></div></section><section className="section shell"><div className="section-heading"><div><p className="eyebrow">Hard controls</p><h2>Safe defaults are part of the commercial design.</h2></div></div><ol className="control-list">{controls.map((control,index)=><li key={control}><span>0{index+1}</span>{control}</li>)}</ol></section><ProductCTA title="Define the data boundary before choosing a model."/><ProductSchema path="/revenueos/ai" name="RevenueOS Optional AI Architecture" description="Provider-independent AI assistance with deterministic records and human approval."/></>}
